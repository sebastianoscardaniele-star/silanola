import { useState } from 'react';export default function App(){const [items,setItems]=useState(Array.from({length:20},(_,i)=>({id:i+1,have:false,name:''})));return <div style={{padding:20}}><h1>Panini 2026</h1>{items.map(s=><div key={s.id}><button onClick={()=>setItems(items.map(x=>x.id===s.id?{...x,have:!x.have}:x))}>{s.have?'✔':'❌'} {s.id}</button><input value={s.name} onChange={e=>setItems(items.map(x=>x.id===s.id?{...x,name:e.target.value}:x))} /></div>)}</div>}